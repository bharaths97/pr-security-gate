name: PR Security Gate

on:
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  contents: read
  pull-requests: write

jobs:
  security-scan:
    uses: ./.github/workflows/pr-security-gate-reusable.yml
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
